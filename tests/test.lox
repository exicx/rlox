// this is a comment
(( )){} // grouping stuff
!*+-/=<> <= == // operators

900..; // should error "No digits after decimal"
900.1.1; // should error "Number contained two or more decimals"
900.; // should error "No digits after decimal"
